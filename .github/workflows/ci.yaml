name: Local Minikube Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build app image
        run: docker build -t broken-app:local ./app

      - name: Load image into Minikube
        run: minikube image load broken-app:local

      - name: Apply Kubernetes secret
        run: kubectl apply -f kubernetes/secretcConf.yaml

      - name: Apply Kubernetes deployment
        run: kubectl apply -f kubernetes/deployment.yaml

      - name: Wait for deployment
        run: kubectl rollout status deployment/broken-app --timeout=180s

      - name: Port-forward app
        run: |
          nohup kubectl port-forward deployment/broken-app 3000:3000 >/tmp/broken-app-portforward.log 2>&1 &
          sleep 10
          curl -fs http://localhost:3000/health

      - name: Show pod status
        run: kubectl get pods
        
